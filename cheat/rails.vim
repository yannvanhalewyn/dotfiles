# Edit alternate file (aka spec <-> whatever it's testing)
:A
:AS (in split)
:AV (in v-split)

# Edit related file (aka view template <-> controller )
:R
:RS (in split)
:RH (in v-split)

# You can call Rake with a task
:Rake {task}
