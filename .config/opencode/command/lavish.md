---
description: Turn complex or visual agent responses into rich, reviewable HTML artifacts via the lavish-axi CLI
---

Load the `lavish` skill with the skill tool, then follow its current guidance (via `npx -y lavish-axi --help` and `npx -y lavish-axi design`) to build the requested artifact and open it for review.

$ARGUMENTS
